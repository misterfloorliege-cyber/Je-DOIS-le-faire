import { useEffect, useMemo, useState } from 'react'
import { Activity, ArrowRight, Bell, CalendarDays, Check, ChevronRight, CircleHelp, Clock3, Coffee, FolderKanban, Gauge, Globe2, LayoutDashboard, ListTodo, LogOut, Menu, Moon, MoreHorizontal, Pause, Play, Plus, Search, Settings, Sparkles, Sun, Timer, TrendingUp, UserRound, X, Zap } from 'lucide-react'
import { signInWithGoogle } from './lib/supabase'

type View = 'dashboard' | 'tasks' | 'timer' | 'calendar' | 'settings'
type Task = { id:number; title:string; project:string; category:'Professionnel'|'Privé'; priority:'Prioritaire'|'Élevée'|'Normale'; done:boolean; color:string }
const initialTasks: Task[] = [
 {id:1,title:'Préparer la présentation client',project:'Refonte du site',category:'Professionnel',priority:'Prioritaire',done:false,color:'#6366f1'},
 {id:2,title:'Réserver les billets de train',project:'Voyage à Lyon',category:'Privé',priority:'Élevée',done:false,color:'#f97316'},
 {id:3,title:'Répondre aux emails importants',project:'Administratif',category:'Professionnel',priority:'Normale',done:true,color:'#22c55e'},
 {id:4,title:'Aller courir 30 minutes',project:'Bien-être',category:'Privé',priority:'Normale',done:false,color:'#ec4899'}
]
const week = [{d:'Lun',h:'6h 20',v:6.3},{d:'Mar',h:'7h 45',v:7.75},{d:'Mer',h:'5h 10',v:5.16},{d:'Jeu',h:'8h 12',v:8.2},{d:'Ven',h:'6h 48',v:6.8},{d:'Sam',h:'3h 24',v:3.4},{d:'Dim',h:'1h 05',v:1.08}]

function App() {
 const [view,setView] = useState<View>('dashboard'); const [dark,setDark] = useState(false); const [menu,setMenu] = useState(false)
 const [tasks,setTasks] = useState<Task[]>(initialTasks); const [running,setRunning] = useState(false); const [seconds,setSeconds] = useState(0); const [showAdd,setShowAdd] = useState(false); const [toast,setToast] = useState('')
 useEffect(()=>{ document.documentElement.classList.toggle('dark',dark) },[dark])
 useEffect(()=>{ if(!running) return; const id=setInterval(()=>setSeconds(s=>s+1),1000); return ()=>clearInterval(id) },[running])
 const elapsed = `${String(Math.floor(seconds/3600)).padStart(2,'0')}:${String(Math.floor(seconds/60)%60).padStart(2,'0')}:${String(seconds%60).padStart(2,'0')}`
 const navigate=(v:View)=>{setView(v);setMenu(false)}
 const complete=(id:number)=>setTasks(ts=>ts.map(t=>t.id===id?{...t,done:!t.done}:t))
 const addTask=(title:string)=>{setTasks(ts=>[{id:Date.now(),title,project:'Nouveau projet',category:'Professionnel',priority:'Normale',done:false,color:'#8b5cf6'},...ts]);setShowAdd(false);setToast('Tâche ajoutée à votre liste');setTimeout(()=>setToast(''),2500)}
 return <div className="min-h-screen bg-[#f7f9fc] text-slate-800 transition-colors dark:bg-slate-950 dark:text-slate-100">
  <aside className={`fixed inset-y-0 left-0 z-30 flex w-[250px] flex-col border-r border-slate-200 bg-white px-4 py-6 transition-transform dark:border-slate-800 dark:bg-slate-900 ${menu?'translate-x-0':'-translate-x-full'} lg:translate-x-0`}>
   <div className="mb-10 flex items-center gap-2 px-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-indigo-600 text-white"><Zap size={20} fill="currentColor"/></div><span className="text-lg font-extrabold tracking-tight">Je DOIS <span className="text-indigo-600">le faire</span></span></div>
   <nav className="space-y-1">{([['dashboard','Vue d’ensemble',LayoutDashboard],['tasks','Mes tâches',ListTodo],['timer','Suivi du temps',Timer],['calendar','Agenda',CalendarDays],['settings','Paramètres',Settings]] as const).map(([id,label,Icon])=><button key={id} onClick={()=>navigate(id)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold transition ${view===id?'bg-indigo-50 text-indigo-700 dark:bg-indigo-500/15 dark:text-indigo-300':'text-slate-500 hover:bg-slate-50 dark:text-slate-400 dark:hover:bg-slate-800'}`}><Icon size={19}/>{label}{id==='tasks'&&<span className="ml-auto rounded-full bg-indigo-100 px-2 py-0.5 text-xs text-indigo-700 dark:bg-indigo-500/20 dark:text-indigo-300">4</span>}</button>)}</nav>
   <div className="mt-auto rounded-2xl bg-gradient-to-br from-indigo-600 to-violet-600 p-4 text-white"><Sparkles size={20}/><p className="mt-3 text-sm font-bold">Votre semaine en équilibre</p><p className="mt-1 text-xs leading-5 text-indigo-100">Vous avez gagné 12% de temps productif cette semaine.</p><button className="mt-3 flex items-center gap-1 text-xs font-bold text-white">Voir les détails <ArrowRight size={13}/></button></div>
   <div className="mt-5 flex items-center gap-3 border-t border-slate-100 pt-5 dark:border-slate-800"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-amber-100 text-sm font-bold text-amber-700">ML</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">Marie Lambert</p><p className="truncate text-xs text-slate-400">marie@exemple.fr</p></div><button onClick={()=>setToast('Menu du compte')}><MoreHorizontal size={17} className="text-slate-400"/></button></div>
  </aside>
  {menu&&<div onClick={()=>setMenu(false)} className="fixed inset-0 z-20 bg-slate-900/30 lg:hidden"/>}
  <main className="lg:ml-[250px]">
   <header className="sticky top-0 z-10 flex h-[76px] items-center justify-between border-b border-slate-200/80 bg-[#f7f9fc]/90 px-5 backdrop-blur md:px-10 dark:border-slate-800 dark:bg-slate-950/90"><div className="flex items-center gap-3"><button className="lg:hidden" onClick={()=>setMenu(true)}><Menu/></button><div className="hidden items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm text-slate-400 md:flex dark:border-slate-700 dark:bg-slate-800"><Search size={16}/><span>Rechercher...</span><kbd className="ml-8 rounded bg-slate-100 px-1.5 py-0.5 text-[10px] dark:bg-slate-700">⌘ K</kbd></div><h1 className="text-lg font-extrabold md:hidden">{view==='dashboard'?'Bonjour Marie 👋':view==='tasks'?'Mes tâches':view==='timer'?'Suivi du temps':view==='calendar'?'Agenda':'Paramètres'}</h1></div><div className="flex items-center gap-2"><button onClick={()=>setDark(!dark)} className="rounded-xl p-2.5 text-slate-500 hover:bg-white dark:hover:bg-slate-800">{dark?<Sun size={19}/>:<Moon size={19}/>}</button><button onClick={()=>setToast('Aucune nouvelle notification')} className="relative rounded-xl p-2.5 text-slate-500 hover:bg-white dark:hover:bg-slate-800"><Bell size={19}/><i className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-rose-500"/></button><button onClick={()=>navigate('settings')} className="ml-1 flex h-9 w-9 items-center justify-center rounded-full bg-amber-100 text-sm font-bold text-amber-700">ML</button></div></header>
   <div className="mx-auto max-w-[1400px] px-5 py-8 md:px-10">{view==='dashboard'&&<Dashboard tasks={tasks} complete={complete} running={running} elapsed={elapsed} setRunning={setRunning} navigate={navigate} setShowAdd={setShowAdd}/>} {view==='tasks'&&<Tasks tasks={tasks} complete={complete} setShowAdd={setShowAdd}/>} {view==='timer'&&<TimerPage running={running} setRunning={setRunning} elapsed={elapsed} seconds={seconds}/>} {view==='calendar'&&<CalendarPage toast={setToast}/>} {view==='settings'&&<SettingsPage dark={dark} setDark={setDark}/>}</div>
  </main>
  {showAdd&&<AddModal close={()=>setShowAdd(false)} add={addTask}/>} {toast&&<div className="fixed bottom-6 right-6 z-50 flex items-center gap-2 rounded-xl bg-slate-900 px-4 py-3 text-sm font-semibold text-white shadow-xl"><Check size={17} className="text-emerald-400"/>{toast}</div>}
 </div>
}

function Header({title,subtitle,action}:{title:string;subtitle:string;action?:React.ReactNode}){return <div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-sm font-semibold text-indigo-600">Jeudi 24 septembre 2026</p><h2 className="text-3xl font-extrabold tracking-tight text-slate-900 dark:text-white">{title}</h2><p className="mt-2 text-sm text-slate-500 dark:text-slate-400">{subtitle}</p></div>{action}</div>}
function Dashboard({tasks,complete,running,elapsed,setRunning,navigate,setShowAdd}:{tasks:Task[];complete:(id:number)=>void;running:boolean;elapsed:string;setRunning:(x:boolean)=>void;navigate:(v:View)=>void;setShowAdd:(x:boolean)=>void}){const done=tasks.filter(t=>t.done).length; return <><Header title="Bonjour Marie 👋" subtitle="Voici ce qui se passe dans votre journée." action={<button className="btn-primary" onClick={()=>setShowAdd(true)}><Plus size={18}/> Nouvelle tâche</button>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={Clock3} label="Temps total" value="38h 44m" detail="+12% vs semaine dernière" color="indigo"/><Stat icon={FolderKanban} label="Projets actifs" value="06" detail="2 projets prioritaires" color="orange"/><Stat icon={Check} label="Tâches terminées" value={`${done} / ${tasks.length}`} detail="Excellent rythme !" color="emerald"/><Stat icon={TrendingUp} label="Productivité" value="84%" detail="+8% cette semaine" color="pink"/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.4fr_1fr]"><div className="card p-5 md:p-6"><div className="mb-6 flex items-center justify-between"><div><h3 className="font-extrabold">Votre activité</h3><p className="mt-1 text-xs muted">Temps suivi cette semaine</p></div><select className="rounded-lg border-0 bg-slate-50 px-3 py-2 text-xs font-semibold outline-none dark:bg-slate-700"><option>Cette semaine</option><option>Ce mois-ci</option></select></div><div className="flex h-56 items-end gap-2 sm:gap-5">{week.map((x,i)=><div key={x.d} className="flex h-full flex-1 flex-col items-center justify-end gap-2"><div className={`w-full max-w-[42px] rounded-t-lg transition ${i===3?'bg-indigo-600':'bg-indigo-100 dark:bg-indigo-500/25'}`} style={{height:`${Math.max(12,x.v/8.5*100)}%`}}></div><span className="text-xs font-medium text-slate-400">{x.d}</span><span className="hidden text-[10px] font-bold text-slate-500 sm:block">{x.h}</span></div>)}</div></div><div className="card p-5 md:p-6"><div className="mb-5 flex items-center justify-between"><div><h3 className="font-extrabold">Suivi en cours</h3><p className="mt-1 text-xs muted">Votre session actuelle</p></div><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[11px] font-bold text-emerald-600 dark:bg-emerald-500/10">{running?'EN COURS':'PRÊT'}</span></div><div className="rounded-2xl bg-slate-50 p-5 text-center dark:bg-slate-700/50"><div className="mx-auto mb-3 flex h-12 w-12 items-center justify-center rounded-2xl bg-indigo-100 text-indigo-600 dark:bg-indigo-500/20"><Timer size={24}/></div><p className="text-sm font-bold">{running?'Préparation de la présentation':'Prêt à suivre votre temps'}</p><p className="my-3 font-mono text-3xl font-extrabold tracking-wider">{elapsed}</p><button onClick={()=>setRunning(!running)} className={`mx-auto flex items-center gap-2 rounded-xl px-5 py-2.5 text-sm font-bold text-white ${running?'bg-rose-500 hover:bg-rose-600':'bg-indigo-600 hover:bg-indigo-700'}`}>{running?<><Pause size={16}/> Mettre en pause</>:<><Play size={16} fill="currentColor"/> Démarrer</>}</button></div><button onClick={()=>navigate('timer')} className="mt-4 flex w-full items-center justify-center gap-1 text-xs font-bold text-indigo-600">Voir le suivi détaillé <ChevronRight size={14}/></button></div></div><div className="mt-6 card p-5 md:p-6"><div className="mb-5 flex items-center justify-between"><div><h3 className="font-extrabold">À ne pas oublier</h3><p className="mt-1 text-xs muted">Vos prochaines tâches</p></div><button onClick={()=>navigate('tasks')} className="text-xs font-bold text-indigo-600">Tout voir</button></div><div className="grid gap-3 md:grid-cols-2">{tasks.filter(t=>!t.done).slice(0,4).map(t=><TaskRow key={t.id} task={t} complete={complete}/>)}</div></div></>}
function Stat({icon:Icon,label,value,detail,color}:{icon:any;label:string;value:string;detail:string;color:string}){return <div className="card p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold muted">{label}</p><p className="mt-2 text-2xl font-extrabold">{value}</p></div><div className={`rounded-xl p-2.5 text-${color}-600 bg-${color}-50 dark:bg-${color}-500/10`}><Icon size={19}/></div></div><p className="mt-3 text-xs font-semibold text-emerald-600">{detail}</p></div>}
function TaskRow({task,complete}:{task:Task;complete:(id:number)=>void}){return <div className="flex items-center gap-3 rounded-xl border border-slate-100 p-3 dark:border-slate-700"><button onClick={()=>complete(task.id)} className={`flex h-5 w-5 shrink-0 items-center justify-center rounded-md border-2 ${task.done?'border-emerald-500 bg-emerald-500 text-white':'border-slate-300 dark:border-slate-500'}`}>{task.done&&<Check size={13}/>}</button><div className="min-w-0 flex-1"><p className={`truncate text-sm font-bold ${task.done?'text-slate-400 line-through':''}`}>{task.title}</p><div className="mt-1 flex items-center gap-2 text-[11px] muted"><span className="h-2 w-2 rounded-full" style={{background:task.color}}/>{task.project}<span>•</span>{task.category}</div></div><span className={`hidden rounded-full px-2 py-1 text-[10px] font-bold sm:block ${task.priority==='Prioritaire'?'bg-rose-50 text-rose-600 dark:bg-rose-500/10':task.priority==='Élevée'?'bg-orange-50 text-orange-600 dark:bg-orange-500/10':'bg-slate-100 text-slate-500 dark:bg-slate-700'}`}>{task.priority}</span></div>}
function Tasks({tasks,complete,setShowAdd}:{tasks:Task[];complete:(id:number)=>void;setShowAdd:(x:boolean)=>void}){return <><Header title="Mes tâches" subtitle="Organisez votre quotidien, une action à la fois." action={<button className="btn-primary" onClick={()=>setShowAdd(true)}><Plus size={18}/> Ajouter une tâche</button>}/><div className="mb-5 flex flex-wrap gap-2"><button className="btn-soft">Toutes <span className="ml-1 rounded bg-white px-1.5 text-xs dark:bg-slate-600">{tasks.length}</span></button><button className="btn-soft">Professionnelles</button><button className="btn-soft">Privées</button><button className="btn-soft"><Search size={15}/> Filtrer</button></div><div className="grid gap-3">{tasks.map(t=><TaskRow task={t} complete={complete} key={t.id}/>)}</div></>}
function TimerPage({running,setRunning,elapsed,seconds}:{running:boolean;setRunning:(v:boolean)=>void;elapsed:string;seconds:number}){return <><Header title="Suivi du temps" subtitle="Concentrez-vous sur l’essentiel et mesurez ce qui compte."/><div className="mx-auto max-w-2xl"><div className="card overflow-hidden"><div className="bg-gradient-to-br from-indigo-600 to-violet-600 px-6 py-10 text-center text-white"><p className="text-sm font-semibold text-indigo-100">SESSION ACTUELLE</p><p className="mt-4 font-mono text-6xl font-extrabold tracking-wider">{elapsed}</p><p className="mt-3 text-sm text-indigo-100">{running?'Préparation de la présentation client':'Aucune session en cours'}</p><button onClick={()=>setRunning(!running)} className={`mt-7 inline-flex items-center gap-2 rounded-xl px-6 py-3 font-bold ${running?'bg-white text-rose-600':'bg-white text-indigo-700'}`}>{running?<><Pause size={17}/> Pause</>:<><Play size={17} fill="currentColor"/> Démarrer le suivi</>}</button></div><div className="grid grid-cols-3 divide-x divide-slate-200 p-5 text-center dark:divide-slate-700"><div><p className="text-xl font-extrabold">{Math.floor(seconds/60)||0}m</p><p className="mt-1 text-xs muted">Aujourd’hui</p></div><div><p className="text-xl font-extrabold">38h 44m</p><p className="mt-1 text-xs muted">Cette semaine</p></div><div><p className="text-xl font-extrabold">84%</p><p className="mt-1 text-xs muted">Productivité</p></div></div></div><div className="mt-6 card p-5"><h3 className="font-extrabold">Entrée manuelle</h3><p className="mt-1 text-sm muted">Ajoutez du temps que vous avez déjà consacré à une activité.</p><div className="mt-4 grid gap-3 sm:grid-cols-3"><input placeholder="Projet" className="rounded-xl border border-slate-200 bg-transparent px-3 py-2.5 text-sm outline-none focus:border-indigo-500 dark:border-slate-700"/><input placeholder="Durée (ex. 1h30)" className="rounded-xl border border-slate-200 bg-transparent px-3 py-2.5 text-sm outline-none focus:border-indigo-500 dark:border-slate-700"/><button className="btn-primary" onClick={()=>alert('Entrée enregistrée')}>Enregistrer</button></div></div></div></>}
function CalendarPage({toast}:{toast:(x:string)=>void}){return <><Header title="Votre agenda" subtitle="Retrouvez vos rendez-vous et votre temps au même endroit." action={<button className="btn-primary" onClick={()=>toast('Redirection vers Google Calendar…')}><CalendarDays size={18}/> Connecter Google Calendar</button>}/><div className="card p-6"><div className="mb-6 flex items-center justify-between"><button className="btn-soft">←</button><h3 className="font-extrabold">Septembre 2026</h3><button className="btn-soft">→</button></div><div className="grid grid-cols-7 gap-1 text-center text-xs font-bold text-slate-400">{['Lun','Mar','Mer','Jeu','Ven','Sam','Dim'].map(d=><div key={d} className="py-2">{d}</div>)}{Array.from({length:30},(_,i)=><div key={i} className={`min-h-20 rounded-xl p-2 text-left transition hover:bg-indigo-50 dark:hover:bg-indigo-500/10 ${i===23?'bg-indigo-600 text-white hover:bg-indigo-600':''}`}><span className="text-xs font-bold">{i+1}</span>{[3,8,17,23].includes(i)&&<div className={`mt-2 rounded-md px-1 py-1 text-[10px] font-bold ${i===23?'bg-white/20':'bg-indigo-100 text-indigo-700 dark:bg-indigo-500/20 dark:text-indigo-300'}`}>Réunion</div>}</div>)}</div></div></>}
function SettingsPage({dark,setDark}:{dark:boolean;setDark:(v:boolean)=>void}){return <><Header title="Paramètres" subtitle="Personnalisez votre expérience et vos préférences."/><div className="grid max-w-3xl gap-5"><div className="card p-6"><h3 className="font-extrabold">Profil</h3><div className="mt-5 flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-amber-100 text-lg font-bold text-amber-700">ML</div><div><p className="font-bold">Marie Lambert</p><p className="text-sm muted">marie@exemple.fr</p></div><button className="btn-soft ml-auto">Modifier</button></div></div><div className="card divide-y divide-slate-100 dark:divide-slate-700"><div className="flex items-center gap-4 p-6"><div className="rounded-xl bg-indigo-50 p-3 text-indigo-600 dark:bg-indigo-500/10"><Moon size={19}/></div><div className="flex-1"><p className="font-bold">Mode sombre</p><p className="mt-1 text-xs muted">Confort visuel en toute circonstance</p></div><button onClick={()=>setDark(!dark)} className={`h-6 w-11 rounded-full p-1 transition ${dark?'bg-indigo-600':'bg-slate-300'}`}><span className={`block h-4 w-4 rounded-full bg-white transition ${dark?'translate-x-5':''}`}/></button></div><div className="flex items-center gap-4 p-6"><div className="rounded-xl bg-emerald-50 p-3 text-emerald-600 dark:bg-emerald-500/10"><Bell size={19}/></div><div className="flex-1"><p className="font-bold">Notifications</p><p className="mt-1 text-xs muted">Rappels de tâches et suivi du temps</p></div><span className="text-sm font-semibold text-emerald-600">Activées</span></div><div className="flex items-center gap-4 p-6"><div className="rounded-xl bg-orange-50 p-3 text-orange-600 dark:bg-orange-500/10"><Globe2 size={19}/></div><div className="flex-1"><p className="font-bold">Google Calendar</p><p className="mt-1 text-xs muted">Synchronisez vos événements personnels</p></div><button className="btn-soft">Connecter</button></div></div><button className="flex items-center gap-2 px-2 text-sm font-bold text-rose-500"><LogOut size={17}/> Se déconnecter</button></div></>}
function AddModal({close,add}:{close:()=>void;add:(x:string)=>void}){const [value,setValue]=useState('');return <div className="fixed inset-0 z-40 flex items-center justify-center bg-slate-900/40 p-4"><div className="card w-full max-w-md p-6"><div className="flex items-center justify-between"><h3 className="text-lg font-extrabold">Nouvelle tâche</h3><button onClick={close}><X size={20} className="muted"/></button></div><input autoFocus value={value} onChange={e=>setValue(e.target.value)} onKeyDown={e=>e.key==='Enter'&&value&&add(value)} placeholder="Que devez-vous faire ?" className="mt-6 w-full rounded-xl border border-slate-200 bg-transparent px-4 py-3 outline-none focus:border-indigo-500 dark:border-slate-700"/><div className="mt-5 flex justify-end gap-2"><button className="btn-soft" onClick={close}>Annuler</button><button className="btn-primary" disabled={!value} onClick={()=>add(value)}>Ajouter</button></div></div></div>}

export default App
